[project]
name = "accept"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "backend",
    "pytest>=9.0.2",
]

[tool.uv.sources]
backend = { path = "../backend", editable = true }
